# @package _group_

fairseq:
  task:
    _name: unpaired_audio_text
    labels: phn
    data: ???
    sort_by_length: false
    shuffle: false
    text_data: ''
    # Phone KenLM .bin (same as GAN training); used by w2vu_generate to score hypotheses (LM PPL).
    kenlm_path: null

  common_eval:
    path: ???
    quiet: true

  dataset:
    gen_subset: valid
    batch_size: 1

w2l_decoder: VITERBI
post_process: silence
